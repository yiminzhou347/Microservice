server:
  port: 9002

#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka/
#  instance:
#    hostname: localhost

spring:
  cloud:
    config:
      enabled: false
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/genspark
    username: root
    password: 1234
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update


